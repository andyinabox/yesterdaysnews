package imageprocessor

import (
	"context"
	"errors"
)

func (p *Processor) OverlayImages(ctx context.Context, baseFile, overlayFile, outputFile string) (string, error) {
	return "", errors.New("not implemented")
}
